---
before:
  app/models/post.rb: |
    $ == Schema Info
    $
    $ Table name: posts
    $
    $   id         integer    not null, primary key
    $   content    text
    $

    $ This is a blog entry
    class Post < ActiveRecord::Base
    end
after:
  app/models/post.rb: |
    $ == Schema Information
    $
    $ Table name: posts
    $
    $   id         integer    not null, primary key
    $   content    text
    $

    $ This is a blog entry
    class Post < ActiveRecord::Base
    end
